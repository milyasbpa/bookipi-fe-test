import { PlayerContainer } from '@/features/quiz-player';

/**
 * Quiz Player Page
 * 
 * Route: /quiz-maker/player/[quizId]
 * Purpose: Take a quiz (player view)
 */
export default function PlayerPage() {
  return <PlayerContainer />;
}
